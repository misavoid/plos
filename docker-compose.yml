services:
  db:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_USER: plos
      POSTGRES_PASSWORD: secret
      POSTGRES_DB: plos_dev
    ports:
      - "5432:5432"
    volumes:
      - db-data:/var/lib/postgresql/data

volumes:
  db-data: